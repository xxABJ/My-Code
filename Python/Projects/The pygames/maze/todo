• USE self._chosen_cell_info and self._selected_cell_info in maze.py

• Fix assignment logics (assignment.py, right.py(90%), left.py, up.py, down.py)
• @@@@ FIX assignment.py.__get_amount_of_assignments

• Fix printing in general to make it useful without random debuging prints, only useful ones 
☻☻ DONE:
- r (prev r) movements! <- check

• USE MazeEngine() !



• Cache system (a, b, ):
a- static total cache_assignments = 5, to produce this system, some logic has to be implemented that would change the surrounding assignments with a block-radius to allow for the system to know when it should stop looping through the choosen number of assignments. 
-  raduis can be two types of blocks (to account for the randomly generated walls in between)
- i.e. RED for one cell away from assignment, ORANGE for two cells away from assignment, GREEN for >= 3 away from assignments.

b- From a , it should make it easier for the cache system to jump back to specific locations (cache_assignments) to soft-unlock by:
- change directions
- or go even further back





• After solid assignments and logging logics, figure out how the maze will be generated:
- scanner 3x3?
- last direction value? (Need to be fixed first)
- Or a cached set of moves - limited? or unlimited in range? - to undo/recap when self locked
-


• Remove unwanted code


• Reformat ( mazelogic.py -> [translations?, objects?, etc] )?

